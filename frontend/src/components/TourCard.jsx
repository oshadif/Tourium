import{Link}from"react-router-dom";import{money}from"../api";export default function TourCard({tour}){return <article className="tour-card"><img src={tour.image_url} alt={tour.title}/><div className="tour-card-body"><div className="tour-meta"><span>{tour.destination}</span><span>{tour.duration_days} days</span></div><h3>{tour.title}</h3><p>{tour.short_description}</p><div className="rating">★ {Number(tour.average_rating||0).toFixed(1)} ({tour.review_count||0})</div><div className="tour-footer"><div><small>From</small><strong>{money(tour.price_lkr)}</strong></div><Link className="button" to={`/tours/${tour.slug}`}>View Tour</Link></div></div></article>}