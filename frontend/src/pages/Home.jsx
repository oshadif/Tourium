import{useEffect,useState}from"react";import{api}from"../api";import TourCard from"../components/TourCard";
export default function Home(){const[tours,setTours]=useState([]);const[search,setSearch]=useState("");const[error,setError]=useState("");
useEffect(()=>{const t=setTimeout(()=>api(`/tours?search=${encodeURIComponent(search)}`).then(setTours).catch(e=>setError(e.message)),250);return()=>clearTimeout(t)},[search]);
return <main><section className="hero"><div><p className="eyebrow">Explore Sri Lanka</p><h1>Journeys designed around real experiences.</h1><p>Discover curated tours with clear pricing, flexible options, and easy online booking.</p><div className="search-box"><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search Ella, Galle, Sigiriya..."/></div></div></section><section className="section"><div className="section-heading"><div><p className="eyebrow">Featured tours</p><h2>Choose your next escape</h2></div><p>{tours.length} tours available</p></div>{error&&<p className="error">{error}</p>}<div className="tour-grid">{tours.map(t=><TourCard key={t.id} tour={t}/>)}</div></section></main>}
