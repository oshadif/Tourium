import{useState}from"react";import{Link,useNavigate}from"react-router-dom";import{api}from"../api";
export default function Register(){const nav=useNavigate();const[form,setForm]=useState({name:"",email:"",phone:"",password:""});const[error,setError]=useState("");async function submit(e){e.preventDefault();try{const d=await api("/auth/register",{method:"POST",body:JSON.stringify(form)});localStorage.setItem("token",d.token);localStorage.setItem("user",JSON.stringify(d.user));nav("/");location.reload()}catch(x){setError(x.message)}}return <main className="auth-page"><form className="auth-card" onSubmit={submit}><p className="eyebrow">Start exploring</p><h1>Create account</h1>{["name","email","phone","password"].map(k=><label key={k}>{k[0].toUpperCase()+k.slice(1)}<input type={k==="password"?"password":k==="email"?"email":"text"} required={k!=="phone"} minLength={k==="password"?6:undefined} onChange={e=>setForm({...form,[k]:e.target.value})}/></label>)}<button className="button full">Register</button>{error&&<p className="error">{error}</p>}<p>Already registered? <Link to="/login">Login</Link></p></form></main>}
